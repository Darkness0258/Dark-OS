diff --git a/airootfs/etc/xdg/hypr/hyprland.conf b/airootfs/etc/xdg/hypr/hyprland.conf
index 392b171..076d1b7 100644
--- a/airootfs/etc/xdg/hypr/hyprland.conf
+++ b/airootfs/etc/xdg/hypr/hyprland.conf
@@ -11,6 +11,10 @@ exec-once = swaybg -i /usr/share/backgrounds/darkos/darkos-wallpaper.png -m fill
 exec-once = waybar
 exec-once = mako
 exec-once = python /usr/local/bin/darkos-shell.py
+# Shield continuous protection: watches ~/Downloads and ~/Desktop, auto-
+# quarantines anything ClamAV flags. Needs mako (already above) to alert
+# on a detection -- see darkos-protect.py's module docstring for scope.
+exec-once = python /usr/local/bin/darkos-protect.py
 exec-once = hypridle
 exec-once = nm-applet --indicator
 exec-once = blueman-applet
diff --git a/airootfs/usr/local/bin/darkos-protect.py b/airootfs/usr/local/bin/darkos-protect.py
index a68223d..e40db69 100644
--- a/airootfs/usr/local/bin/darkos-protect.py
+++ b/airootfs/usr/local/bin/darkos-protect.py
@@ -18,16 +18,19 @@ Scope, stated plainly rather than left implicit:
     this passive watcher can stand in for, since pacman's own cache isn't
     a user-writable directory this can watch the same way.
 
-Runs as the desktop user, not root -- inotify doesn't need CAP_SYS_ADMIN,
-so unlike a fanotify-based version would have needed, this doesn't require
-its own privileged systemd service. A per-user `systemd --user` unit is
-the natural fit; not written here since it depends on the unit-file
-conventions the rest of this repo's systemd units use, which is worth
-matching rather than guessing at in isolation.
+Runs as the desktop user, not root -- inotify doesn't need CAP_SYS_ADMIN.
+Launched via hyprland.conf's exec-once, the same mechanism every other
+session daemon here uses (waybar, mako, hypridle, darkos-shell.py itself)
+-- checked start-hyprland first rather than assuming systemd --user was
+available: this project's session is tty1-autologin straight into
+Hyprland, no display manager, no systemd --user session ever gets
+started. A --user unit would have silently never run.
 """
 
 from __future__ import annotations
 
+import logging
+import subprocess
 import sys
 import threading
 import time
@@ -39,6 +42,41 @@ from darkos_shell.continuous_watch import ContinuousWatcher, InotifyUnavailable
 from darkos_shell.shield import scan_and_quarantine
 
 WATCH_PATHS = [Path.home() / "Downloads", Path.home() / "Desktop"]
+LOG_PATH = Path.home() / ".local" / "share" / "darkos" / "protect.log"
+
+log = logging.getLogger("darkos-protect")
+
+
+def _setup_logging() -> None:
+    """A real log file, not just stdout -- exec-once doesn't redirect
+    stdout anywhere durable (same gap ci/hardware-audit.sh already
+    documented for darkos-shell.py itself), and this is a security-relevant
+    daemon: losing its output on every session restart isn't acceptable
+    the way it might be for a cosmetic one."""
+    LOG_PATH.parent.mkdir(parents=True, exist_ok=True)
+    handler = logging.FileHandler(LOG_PATH)
+    handler.setFormatter(logging.Formatter("%(asctime)s %(message)s"))
+    log.addHandler(handler)
+    log.addHandler(logging.StreamHandler(sys.stdout))
+    log.setLevel(logging.INFO)
+
+
+def _notify(summary: str, body: str, urgency: str = "normal") -> None:
+    """Best-effort desktop notification via notify-send -> mako, already
+    part of this session's exec-once stack. A quarantine the user never
+    sees isn't much better than one that never happened -- this is what
+    actually closes that gap, not just the log file. Deliberately can't
+    fail the caller: no notification daemon reachable yet (e.g. very
+    early in session startup) is routine, not an error worth surfacing
+    as one."""
+    try:
+        subprocess.run(
+            ["notify-send", "--urgency", urgency, summary, body],
+            timeout=5,
+            capture_output=True,
+        )
+    except (OSError, subprocess.TimeoutExpired) as error:
+        log.info("notify-send unavailable (%s) -- continuing without it", error)
 
 
 def _on_file_ready(path: Path) -> None:
@@ -46,23 +84,29 @@ def _on_file_ready(path: Path) -> None:
     result, actions = scan_and_quarantine(path, cancel)
     if result.outcome == "detected":
         for action in actions:
-            print(f"[darkos-protect] {action}", flush=True)
+            log.warning(action)
+        _notify(
+            "DarkOS Shield",
+            f"Quarantined {path.name} -- open Security Center to review or restore.",
+            urgency="critical",
+        )
     elif result.outcome == "error":
         # Errors here are routine (permission-denied on a file that
         # vanished between the close event and the scan starting, e.g.
         # a browser's temp download artifact) -- logged, not fatal to
-        # the watcher itself.
-        print(f"[darkos-protect] scan error on {path}: {result.detail}", flush=True)
+        # the watcher itself, and not worth a notification.
+        log.info("scan error on %s: %s", path, result.detail)
 
 
 def main() -> int:
+    _setup_logging()
     watcher = ContinuousWatcher(WATCH_PATHS, _on_file_ready)
     try:
         watcher.start()
     except InotifyUnavailable as error:
-        print(f"[darkos-protect] cannot start: {error}", file=sys.stderr, flush=True)
+        log.error("cannot start: %s", error)
         return 1
-    print(f"[darkos-protect] watching: {[str(p) for p in WATCH_PATHS]}", flush=True)
+    log.info("watching: %s", [str(p) for p in WATCH_PATHS])
     try:
         while True:
             time.sleep(30)
diff --git a/build-plan.md b/build-plan.md
index 2c26d44..2a55757 100644
--- a/build-plan.md
+++ b/build-plan.md
@@ -66,7 +66,7 @@ Goal — Settings actually manages the system. This is where most of the origina
 - [x] Network Center (Wi-Fi, Bluetooth, Connect, Cloud) **code complete 2026-08-29** — `darkos-network.py`. Wi-Fi/Bluetooth shell out to `nmcli`/`bluetoothctl` (the correct approach — NetworkManager/BlueZ own real hardware state, this doesn't reimplement that) and report the real failure rather than a fake device list. **Runtime-verified against two different real failure modes** in this sandbox: `nmcli` exits cleanly with "Could not connect: No such file or directory" (no NetworkManager running) and `bluetoothctl` actually aborts outright (SIGABRT, no D-Bus) — confirmed the app catches both without crashing itself and shows the real error text either way. Cloud tab is a stub (nothing exists to connect to before Phase 9).
 - [~] Connect (Network Center tab), **integrated 2026-09-06** — upstream `kdeconnect` owns discovery, TLS pairing, and plugins. The native tab reads typed D-Bus device status and offers confirmed pair/unpair, ring, and file sharing; notification/clipboard permissions remain in upstream settings. Thirteen hardware-free regressions and real GTK startup pass. Actual pairing and transfers still require a phone and a desktop session.
 - [~] Security Center (Vault, Privacy, Shield, Permissions, Encrypt) **partially complete 2026-08-30** — `darkos-security.py`, Vault and Encrypt operational, with remaining security scope tracked below: Vault and Encrypt use real, standard primitives (PBKDF2-HMAC-SHA256, 480,000 iterations + Fernet authenticated encryption from the `cryptography` library — not homemade crypto). **Runtime-verified end to end, including the failure paths that actually matter for a security tool, not just the happy path:** created a vault, added a real entry, locked it, unlocked with the correct password (succeeded) and a wrong password (correctly rejected, vault stayed locked) — confirmed via screenshots of both outcomes. Encrypted a real test file, confirmed the output is genuine ciphertext (checked the plaintext bytes do not appear anywhere in the encrypted file), decrypted with the correct passphrase and got back byte-identical content, then decrypted with a wrong passphrase and confirmed it's rejected with no corrupted output file ever written. Privacy tab is real saved toggles (staged for future enforcement, stated as such — DarkOS has no telemetry to begin with). Permissions tab mirrors Settings' own honest stub. **Update 2026-09-06:** Shield now has real on-demand scanning, described below; continuous protection and the Store gate are still open. Vault creation/locking, private atomic writes, failed-password handling, and output overwrite refusal now also have repeatable automated regressions.
-- [~] Shield, **on-demand scanning implemented 2026-09-06, quarantine added 2026-09-11, continuous protection added 2026-09-11** — cancellable ClamAV file/folder scans, bounded diagnostics, explicit failure/inspection-limit handling, and a user-triggered definitions updater. A detected result parses each hit out of ClamAV's real output (`parse_infected_files`) and moves it to quarantine (`quarantine.py` — move/restore/list/permanent-delete, one-click restore per architecture.md, never a silent delete, ownership-preserving for a privileged caller) instead of just reporting it — shared by both the on-demand scan and continuous protection via `scan_and_quarantine`, one code path, not two. Continuous protection (`continuous_watch.py` + `darkos-protect.py`) watches `~/Downloads` and `~/Desktop` and auto-scans anything dropped in. Built against fanotify first, per this line's original wording — switched to inotify after `fanotify_init`/`fanotify_mark` reported success but never once delivered a real event in testing, confirmed against a real file write with a blocking read, while the identical test against inotify worked immediately. inotify also doesn't need CAP_SYS_ADMIN, so this runs as the desktop user, not a separate root daemon. 27 regressions pass across `ci/test-shield.py` and the new `ci/test-continuous-protection.py`, including a real end-to-end run: real file drop, real inotify event, real ClamAV detection, real quarantine, with a genuinely clean file in the same batch correctly left alone. Not yet covered: removable-media mount points (inotify doesn't auto-discover new mounts under a watched parent — needs a udev/poll loop calling `rescan()`, not written), rkhunter/AIDE baselines, the Store installation scan gate, and a systemd unit to actually run `darkos-protect.py` persistently.
+- [~] Shield, **on-demand scanning implemented 2026-09-06, quarantine added 2026-09-11, continuous protection added 2026-09-11** — cancellable ClamAV file/folder scans, bounded diagnostics, explicit failure/inspection-limit handling, and a user-triggered definitions updater. A detected result parses each hit out of ClamAV's real output (`parse_infected_files`) and moves it to quarantine (`quarantine.py` — move/restore/list/permanent-delete, one-click restore per architecture.md, never a silent delete, ownership-preserving for a privileged caller) instead of just reporting it — shared by both the on-demand scan and continuous protection via `scan_and_quarantine`, one code path, not two. Continuous protection (`continuous_watch.py` + `darkos-protect.py`) watches `~/Downloads` and `~/Desktop` and auto-scans anything dropped in. Built against fanotify first, per this line's original wording — switched to inotify after `fanotify_init`/`fanotify_mark` reported success but never once delivered a real event in testing, confirmed against a real file write with a blocking read, while the identical test against inotify worked immediately. inotify also doesn't need CAP_SYS_ADMIN, so this runs as the desktop user, not a separate root daemon. Launches via `hyprland.conf`'s `exec-once` (checked `start-hyprland` first: this session is tty1-autologin straight into Hyprland, no display manager, no systemd --user session ever starts — a `--user` unit, this line's earlier guess at how it'd be launched, would have silently never run), logs to `~/.local/share/darkos/protect.log`, and alerts through `notify-send` → mako on a detection rather than only writing a log line nobody's watching (`libnotify` added to `packages.x86_64` for this). 27 regressions pass across `ci/test-shield.py` and `ci/test-continuous-protection.py`, including a real end-to-end run: real file drop, real inotify event, real ClamAV detection, real quarantine, with a genuinely clean file in the same batch correctly left alone. Not yet covered: removable-media mount points (inotify doesn't auto-discover new mounts under a watched parent — needs a udev/poll loop calling `rescan()`, not written), rkhunter/AIDE baselines, and the Store installation scan gate.
 - [ ] Network transparency dashboard (per-app, plain-language) — reuses PHANTOM's monitoring core. **Blocked on input, not effort:** PHANTOM's source isn't in this repo or this conversation — reusing its actual monitoring core means either Hamza sharing that code, or reimplementing something PHANTOM-*like* from scratch, which risks quietly diverging from what "reuse" was meant to mean here.
 - [x] Backup / Recovery **code complete 2026-08-30** — `darkos-backup.py`. Plain file-based backup (Python `tarfile`, not Btrfs snapshots — this sandbox's filesystem is ext2/ext3 so a snapshot approach couldn't be verified here regardless, and the spec didn't call for one specifically). A manifest at `~/.local/share/darkos/backups/manifest.json` tracks what was backed up, when, and how big it was. **Runtime-verified end to end:** backed up a real test folder through the actual UI, confirmed the resulting `.tar.gz` has correct internal paths (`tar -tzf`), then restored it and diffed the restored file against the original — byte-identical. `tarfile.extractall(..., filter="data")` (PEP 706) same as File Explorer's archive handling, guarding against path-traversal from an archive of unknown origin.
 - [x] Dashboard (Performance, Overlay); [x] Mission/Spaces — **both done 2026-08-30.** `darkos-dashboard.py`: live CPU% (computed from two `/proc/stat` samples a tick apart, the standard technique), memory, disk, and top-processes-by-memory, refreshing every 2 seconds. **Runtime-verified with an actual load test** — ran a real CPU-bound busy-loop in the background and confirmed the dashboard's CPU reading actually moved from 0% to 100% between ticks, and that the busy-loop's own process appeared in the live top-processes list. `darkos-mission.py`: real `hyprctl -j workspaces` / `hyprctl -j clients` calls, grouped by workspace, with real `hyprctl dispatch` actions to switch workspace or focus a window. **Correction from earlier in this same session:** this was first bucketed with Shield as "can't verify here" — that was too pessimistic. Shield's correctness is fundamentally unverifiable without a real scan engine; this is a data-display problem against hyprctl's documented, stable JSON schema, which is a fundamentally different and much more tractable category — same shape as Network Center's nmcli/bluetoothctl calls. Verified two separate things, both for real: (1) the graceful-failure path, which is genuinely real in this sandbox (`hyprctl is not installed`, confirmed via screenshot), and (2) the parsing/rendering logic, via a schema-accurate fake `hyprctl` script on `PATH` returning realistic workspace/client JSON (documented format, not invented) — confirmed correct grouping including an empty-workspace case, correct unicode title decoding, and confirmed the dispatch buttons don't crash the app when clicked. What's still unverified is purely cosmetic/compositor-specific (does it look right against real windows) — the logic itself has been checked both ways it can be.
diff --git a/packages.x86_64 b/packages.x86_64
index 36079ec..b18f065 100644
--- a/packages.x86_64
+++ b/packages.x86_64
@@ -94,6 +94,7 @@ adwaita-icon-theme
 gtk-layer-shell
 swaybg
 mako
+libnotify
 brightnessctl
 pamixer
 playerctl
