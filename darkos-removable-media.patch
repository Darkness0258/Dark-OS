diff --git a/airootfs/usr/local/bin/darkos-protect.py b/airootfs/usr/local/bin/darkos-protect.py
index 5d24833..fbfa037 100644
--- a/airootfs/usr/local/bin/darkos-protect.py
+++ b/airootfs/usr/local/bin/darkos-protect.py
@@ -6,13 +6,11 @@ dropped into them, closing the loop architecture.md describes: watch,
 scan, quarantine -- automatically, without a manual on-demand scan.
 
 Scope, stated plainly rather than left implicit:
-  - Covers ~/Downloads and ~/Desktop. Solid, verified (see
-    ci/test-continuous-protection.py), safe to rely on.
-  - Does NOT yet cover removable media. Watching new mounts needs
-    detecting them as they appear (a udev/polling loop) and marking each
-    mount's own directory -- inotify doesn't watch a parent and
-    automatically pick up new subdirectories on its own. Real gap, not
-    wired here; rescan() exists for exactly this once it is.
+  - Covers ~/Downloads and ~/Desktop, plus removable media discovered
+    under /run/media/$USER and /media/$USER (checked every ~30s, same
+    cycle as the settings toggle -- see discover_mount_dirs()'s own note
+    that this returns nothing at all if udisks2 isn't installed, which is
+    a real, separate prerequisite this doesn't itself provide).
   - Does NOT replace the Store gate (build-plan.md Shield item, still
     open) -- that's an explicit scan-before-install hook, not something
     this passive watcher can stand in for, since pacman's own cache isn't
@@ -38,7 +36,11 @@ from pathlib import Path
 
 sys.path.insert(0, "/usr/local/bin")
 
-from darkos_shell.continuous_watch import ContinuousWatcher, InotifyUnavailable
+from darkos_shell.continuous_watch import (
+    ContinuousWatcher,
+    InotifyUnavailable,
+    discover_mount_dirs,
+)
 from darkos_shell.shield import scan_and_quarantine
 
 WATCH_PATHS = [Path.home() / "Downloads", Path.home() / "Desktop"]
@@ -121,6 +123,8 @@ def _reconcile(watcher: ContinuousWatcher, running: bool) -> bool:
         return False
     if running:
         watcher.rescan()  # picks up a Downloads/Desktop that didn't exist at start
+        for mount_dir in discover_mount_dirs():
+            watcher.add_watch_path(mount_dir)  # no-op if already tracked
     return running
 
 
diff --git a/airootfs/usr/local/bin/darkos_shell/continuous_watch.py b/airootfs/usr/local/bin/darkos_shell/continuous_watch.py
index 980b059..550ce38 100644
--- a/airootfs/usr/local/bin/darkos_shell/continuous_watch.py
+++ b/airootfs/usr/local/bin/darkos_shell/continuous_watch.py
@@ -67,6 +67,22 @@ class InotifyUnavailable(RuntimeError):
     """Raised when inotify_init1 fails."""
 
 
+def discover_mount_dirs() -> list[Path]:
+    """Currently-mounted removable media, checked under both the modern
+    udisks2 convention (/run/media/$USER/<label>) and the older one
+    (/media/$USER/<label>) -- real directories only, symlinks/junk
+    filtered by the is_dir() check callers already do via add_watch_path.
+    Returns [] rather than raising if neither parent exists (e.g. udisks2
+    isn't installed) -- that's a real, separate gap, not this function's
+    to raise an error over."""
+    user = os.environ.get("USER") or os.environ.get("LOGNAME") or ""
+    found: list[Path] = []
+    for parent in (Path("/run/media") / user, Path("/media") / user):
+        if parent.is_dir():
+            found.extend(p for p in parent.iterdir() if p.is_dir())
+    return found
+
+
 class ContinuousWatcher:
     """Watches `watch_paths` (each marked individually, non-recursively --
     a subdirectory created after start() needs rescan() to pick up) and
@@ -103,6 +119,16 @@ class ContinuousWatcher:
             if wd >= 0:
                 self._wd_to_dir[wd] = path
 
+    def add_watch_path(self, path: Path) -> None:
+        """Add one more directory to watch after start() -- e.g. a
+        removable-media mount just discovered by discover_mount_dirs().
+        Marks it immediately if the watcher's already running; if `path`
+        is already tracked, this is a no-op, not a duplicate mark."""
+        path = Path(path)
+        if path not in self._watch_paths:
+            self._watch_paths.append(path)
+        self.rescan()
+
     def stop(self) -> None:
         self._stop_event.set()
         if self._thread is not None:
diff --git a/build-plan.md b/build-plan.md
index b062849..e93a9d0 100644
--- a/build-plan.md
+++ b/build-plan.md
@@ -71,7 +71,8 @@ Goal — Settings actually manages the system. This is where most of the origina
   - **Continuous protection** (`continuous_watch.py` + `darkos-protect.py`): watches `~/Downloads` and `~/Desktop`. Built against fanotify first, per this line's original wording — switched to inotify after `fanotify_init`/`fanotify_mark` reported success but never once delivered a real event in testing (confirmed with a blocking read against a real file write). inotify also needs no CAP_SYS_ADMIN, so this runs as the desktop user, not a root daemon. Launches via `hyprland.conf`'s `exec-once` (checked `start-hyprland` first: tty1-autologin straight into Hyprland, no systemd --user session ever starts — a `--user` unit would have silently never run). Logs to `~/.local/share/darkos/protect.log`, alerts through `notify-send` → mako on a detection (`libnotify` added to `packages.x86_64`).
   - **On/off toggle**: a switch in Security Center's Shield tab, backed by `user_settings.py`'s `shield_protection_enabled` (same settings.json every other preference already uses). `darkos-protect.py` checks it every ~30s via a `_reconcile` step and starts/stops the real watcher accordingly — takes effect within that window, not instantly, stated as the real trade-off it is rather than built to look instant and not be.
   - **Review/restore UI**: a Quarantine tab in Security Center, real entries, Restore/Delete-permanently wired to the real calls. Surfaced two more real bugs by actually rendering it: the same GTK3 stock-chrome gap on `Gtk.ListBox`/`row` that `scale`/`levelbar`/`progressbar` already had (fixed the same way), and an unellipsized path overflow that pushed the action buttons off the visible row (fixed, and now has a regression test specifically because it happened).
-  - **Not yet covered**: removable-media mount points (inotify doesn't auto-discover new mounts under a watched parent — needs a udev/poll loop calling `rescan()`), rkhunter/AIDE baselines, the Store installation scan gate.
+  - **Removable media (2026-09-11)**: `discover_mount_dirs()` checks `/run/media/$USER` and `/media/$USER` every ~30s (same cycle as the toggle); anything found gets added via `ContinuousWatcher.add_watch_path()`. Found a real foundational gap while scoping this: neither `udisks2` nor `gvfs` were in `packages.x86_64` at all, so removable media wasn't auto-mounting in the first place — added both, since watching a mount point nothing ever populates isn't real coverage. This surfaced a genuine, non-obvious testing pitfall too: `GLib.get_user_config_dir()` (what `user_settings.py`'s `settings_path()` calls) caches its result for the whole process after the first call, so patching `$HOME` mid-test doesn't work reliably — cost real debugging time to track down, now documented in the test file itself and fixed by patching `settings_path` directly instead, in all three places that needed it.
+  - **Not yet covered**: rkhunter/AIDE baselines, the Store installation scan gate.
 - [ ] Network transparency dashboard (per-app, plain-language) — reuses PHANTOM's monitoring core. **Blocked on input, not effort:** PHANTOM's source isn't in this repo or this conversation — reusing its actual monitoring core means either Hamza sharing that code, or reimplementing something PHANTOM-*like* from scratch, which risks quietly diverging from what "reuse" was meant to mean here.
 - [x] Backup / Recovery **code complete 2026-08-30** — `darkos-backup.py`. Plain file-based backup (Python `tarfile`, not Btrfs snapshots — this sandbox's filesystem is ext2/ext3 so a snapshot approach couldn't be verified here regardless, and the spec didn't call for one specifically). A manifest at `~/.local/share/darkos/backups/manifest.json` tracks what was backed up, when, and how big it was. **Runtime-verified end to end:** backed up a real test folder through the actual UI, confirmed the resulting `.tar.gz` has correct internal paths (`tar -tzf`), then restored it and diffed the restored file against the original — byte-identical. `tarfile.extractall(..., filter="data")` (PEP 706) same as File Explorer's archive handling, guarding against path-traversal from an archive of unknown origin.
 - [x] Dashboard (Performance, Overlay); [x] Mission/Spaces — **both done 2026-08-30.** `darkos-dashboard.py`: live CPU% (computed from two `/proc/stat` samples a tick apart, the standard technique), memory, disk, and top-processes-by-memory, refreshing every 2 seconds. **Runtime-verified with an actual load test** — ran a real CPU-bound busy-loop in the background and confirmed the dashboard's CPU reading actually moved from 0% to 100% between ticks, and that the busy-loop's own process appeared in the live top-processes list. `darkos-mission.py`: real `hyprctl -j workspaces` / `hyprctl -j clients` calls, grouped by workspace, with real `hyprctl dispatch` actions to switch workspace or focus a window. **Correction from earlier in this same session:** this was first bucketed with Shield as "can't verify here" — that was too pessimistic. Shield's correctness is fundamentally unverifiable without a real scan engine; this is a data-display problem against hyprctl's documented, stable JSON schema, which is a fundamentally different and much more tractable category — same shape as Network Center's nmcli/bluetoothctl calls. Verified two separate things, both for real: (1) the graceful-failure path, which is genuinely real in this sandbox (`hyprctl is not installed`, confirmed via screenshot), and (2) the parsing/rendering logic, via a schema-accurate fake `hyprctl` script on `PATH` returning realistic workspace/client JSON (documented format, not invented) — confirmed correct grouping including an empty-workspace case, correct unicode title decoding, and confirmed the dispatch buttons don't crash the app when clicked. What's still unverified is purely cosmetic/compositor-specific (does it look right against real windows) — the logic itself has been checked both ways it can be.
diff --git a/ci/test-continuous-protection.py b/ci/test-continuous-protection.py
index c72d898..0b908c0 100644
--- a/ci/test-continuous-protection.py
+++ b/ci/test-continuous-protection.py
@@ -14,6 +14,7 @@ import tempfile
 import threading
 import time
 import unittest
+import uuid
 from pathlib import Path
 from unittest.mock import patch
 
@@ -144,13 +145,23 @@ class ReconcileToggleTests(unittest.TestCase):
     def setUp(self) -> None:
         self.home = tempfile.mkdtemp()
         self.addCleanup(shutil.rmtree, self.home, ignore_errors=True)
-        self.home_patcher = patch.dict("os.environ", {"HOME": self.home})
-        self.home_patcher.start()
-        self.addCleanup(self.home_patcher.stop)
 
         from darkos_shell import user_settings
 
         self.user_settings = user_settings
+        # NOT env-var HOME patching: GLib.get_user_config_dir() (which
+        # settings_path() calls) caches its result for the process's
+        # whole lifetime after the first call, so a later os.environ
+        # change is silently ignored -- discovered by this exact test
+        # intermittently reading real, unrelated settings state instead
+        # of its own isolated temp dir. Patching settings_path itself
+        # sidesteps the cache entirely.
+        settings_file = Path(self.home) / "settings.json"
+        self.path_patcher = patch.object(
+            user_settings, "settings_path", return_value=str(settings_file)
+        )
+        self.path_patcher.start()
+        self.addCleanup(self.path_patcher.stop)
 
         spec = importlib.util.spec_from_file_location(
             "darkos_protect_test_target", BIN / "darkos-protect.py"
@@ -194,5 +205,74 @@ class ReconcileToggleTests(unittest.TestCase):
         self.assertEqual(self.seen, [], "must not still be catching files once turned off")
 
 
+class MountDiscoveryTests(unittest.TestCase):
+    """discover_mount_dirs and add_watch_path against real directories
+    under /run/media/$USER -- a temp username, cleaned up after, not the
+    real user's actual mount namespace."""
+
+    def setUp(self) -> None:
+        self.test_user = f"darkos-ci-{uuid.uuid4().hex[:8]}"
+        self.home = tempfile.mkdtemp()  # isolates settings.json --
+        # NOT via env-var HOME patching: GLib.get_user_config_dir() caches
+        # its result for the whole process after the first call, so a
+        # later os.environ change is silently ignored (see
+        # ReconcileToggleTests.setUp's longer note -- same fix here).
+        self.env_patcher = patch.dict("os.environ", {"USER": self.test_user})
+        self.env_patcher.start()
+        self.addCleanup(self.env_patcher.stop)
+        self.addCleanup(shutil.rmtree, self.home, ignore_errors=True)
+        self.run_media = Path("/run/media") / self.test_user
+        self.addCleanup(shutil.rmtree, self.run_media, ignore_errors=True)
+
+        from darkos_shell import continuous_watch, user_settings
+
+        self.discover_mount_dirs = continuous_watch.discover_mount_dirs
+        settings_file = Path(self.home) / "settings.json"
+        self.path_patcher = patch.object(
+            user_settings, "settings_path", return_value=str(settings_file)
+        )
+        self.path_patcher.start()
+        self.addCleanup(self.path_patcher.stop)
+
+    def test_empty_before_anything_mounted(self) -> None:
+        self.assertEqual(self.discover_mount_dirs(), [])
+
+    def test_finds_a_real_new_mount_directory(self) -> None:
+        self.run_media.mkdir(parents=True)
+        usb = self.run_media / "TEST_DRIVE"
+        usb.mkdir()
+        self.assertEqual(self.discover_mount_dirs(), [usb])
+
+    def test_returns_empty_not_an_error_when_parent_is_absent(self) -> None:
+        # e.g. udisks2 isn't installed -- a real, separate prerequisite
+        # this function doesn't provide, and shouldn't pretend to.
+        self.assertFalse(self.run_media.exists())
+        self.assertEqual(self.discover_mount_dirs(), [])
+
+    def test_reconcile_discovers_and_genuinely_watches_a_new_mount(self) -> None:
+        self.run_media.mkdir(parents=True)
+        watchdir = Path(tempfile.mkdtemp())
+        self.addCleanup(shutil.rmtree, watchdir, ignore_errors=True)
+        seen: list[Path] = []
+        watcher = ContinuousWatcher([watchdir], lambda p: seen.append(p))
+        self.addCleanup(watcher.stop)
+
+        spec = importlib.util.spec_from_file_location(
+            "darkos_protect_mount_test", BIN / "darkos-protect.py"
+        )
+        assert spec is not None and spec.loader is not None
+        protect = importlib.util.module_from_spec(spec)
+        spec.loader.exec_module(protect)
+
+        running = protect._reconcile(watcher, running=False)
+        usb = self.run_media / "TEST_DRIVE"
+        usb.mkdir()
+        protect._reconcile(watcher, running=running)
+
+        target = usb / "dropped.txt"
+        target.write_bytes(b"x")
+        self.assertTrue(_wait_until(lambda: seen == [target]))
+
+
 if __name__ == "__main__":
     unittest.main(verbosity=2)
diff --git a/ci/test-security-ui.py b/ci/test-security-ui.py
index 64f4560..e182e1a 100644
--- a/ci/test-security-ui.py
+++ b/ci/test-security-ui.py
@@ -33,6 +33,17 @@ except (ImportError, ValueError):
 @unittest.skipUnless(GTK_AVAILABLE, "GTK 3 not available in this environment")
 class QuarantineTabTests(unittest.TestCase):
     def setUp(self) -> None:
+        from darkos_shell import user_settings
+
+        settings_home = tempfile.mkdtemp()
+        self.addCleanup(shutil.rmtree, settings_home, ignore_errors=True)
+        settings_file = Path(settings_home) / "settings.json"
+        self.settings_patcher = patch.object(
+            user_settings, "settings_path", return_value=str(settings_file)
+        )
+        self.settings_patcher.start()
+        self.addCleanup(self.settings_patcher.stop)
+
         spec = importlib.util.spec_from_file_location(
             "darkos_security_test_target", BIN / "darkos-security.py"
         )
@@ -108,14 +119,22 @@ class QuarantineTabTests(unittest.TestCase):
 class ProtectionToggleTests(unittest.TestCase):
     def setUp(self) -> None:
         self.home = tempfile.mkdtemp()
-        self.home_patcher = patch.dict("os.environ", {"HOME": self.home})
-        self.home_patcher.start()
-        self.addCleanup(self.home_patcher.stop)
         self.addCleanup(shutil.rmtree, self.home, ignore_errors=True)
 
         from darkos_shell import user_settings
 
         self.user_settings = user_settings
+        # patch settings_path directly, not $HOME: GLib.get_user_config_dir()
+        # caches its result for the process's lifetime after the first
+        # call, so a later os.environ change is silently ignored -- see
+        # ci/test-continuous-protection.py's ReconcileToggleTests for the
+        # full story of how this was actually found.
+        settings_file = Path(self.home) / "settings.json"
+        self.path_patcher = patch.object(
+            user_settings, "settings_path", return_value=str(settings_file)
+        )
+        self.path_patcher.start()
+        self.addCleanup(self.path_patcher.stop)
 
         spec = importlib.util.spec_from_file_location(
             "darkos_security_test_target2", BIN / "darkos-security.py"
diff --git a/packages.x86_64 b/packages.x86_64
index b18f065..84e5fc8 100644
--- a/packages.x86_64
+++ b/packages.x86_64
@@ -50,6 +50,8 @@ poppler-glib
 wofi
 xdg-desktop-portal-hyprland
 polkit-kde-agent
+udisks2
+gvfs
 qt5-wayland
 qt6-wayland
 calamares
