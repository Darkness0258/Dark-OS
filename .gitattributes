# The profile is built on Linux but commonly edited on Windows. Normalize
# every detected text file so shell scripts, Calamares YAML, systemd units,
# desktop entries, QML, JSON, and Hyprland/Waybar configs cannot acquire CRLF.
* text=auto eol=lf

# Never run binary assets through text conversion.
*.png binary
*.webp binary
