# SPDX-License-Identifier: GPL-3.0-or-later
#
# Welcome screen — requirements that must be met before install proceeds.
# Schema-valid keys only — check/required are string arrays matching
# the enum [storage, ram, power, internet, root, screen]. The old
# object form (- internet: false) was invalid.
#
---
requirements:
  required:
    - ram
    - storage
  requiredStorage: 8
  requiredRam: 2
  check:
    - ram
    - storage
