# SPDX-License-Identifier: GPL-3.0-or-later
#
# GRUB EFI bootloader. Note: efiMountDir must match partition.conf's
# efiMountPoint so the ESP is properly mounted before grub-install runs.
#
---
efiBootLoader: "grub"
kernelSearchPath: "/usr/lib/modules"
kernelName: "vmlinuz-linux"
timeout: "10"
bootloaderEntryName: "DarkOS"
grubInstall: "grub-install"
grubMkconfig: "grub-mkconfig"
grubCfg: "/boot/grub/grub.cfg"
grubProbe: "grub-probe"
efiBootMgr: "efibootmgr"
efiMountDir: "/boot/efi"
installEFIFallback: true
dontChroot: true
