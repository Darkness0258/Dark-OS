# SPDX-License-Identifier: GPL-3.0-or-later
#
# GRUB EFI bootloader. GRUB's grub-mkconfig scans /boot for kernels
# automatically (handles Arch's flat /boot/vmlinuz-linux layout).
# systemd-boot was incompatible -- its module expects a Fedora-style
# kernel path with version subdirectories.
#
---
efiBootLoader: "grub"
kernelSearchPath: "/usr/lib/modules"
kernelName: "vmlinuz-linux"
timeout: "10"
bootloaderEntryName: "DarkOS"
grubInstall: "grub-install"
grubMkconfig: "grub-mkconfig"
grubCfg: "/boot/grub/grub.cfg"
grubProbe: "grub-probe"
efiBootMgr: "efibootmgr"
efiMountDir: "/boot/efi"
installEFIFallback: true
