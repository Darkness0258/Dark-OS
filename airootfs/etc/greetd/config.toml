[terminal]
vt = 1

[default_session]
command = "env GTK_USE_PORTAL=0 GDK_DEBUG=no-portals GDK_DISABLE=dmabuf,vulkan GSK_RENDERER=cairo dbus-run-session cage -s -mlast -d -- regreet --style /etc/greetd/regreet.css"
user = "greeter"
