[Unit]
Description=Run DarkOS snapshot hourly

[Timer]
OnCalendar=hourly
# Same reasoning as darkos-integrity-check.timer: spreads load, avoids
# every snapshot landing on the exact hour on every machine at once.
RandomizedDelaySec=300
Persistent=true

[Install]
WantedBy=timers.target
