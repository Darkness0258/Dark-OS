[Service]
ExecStart=
# Re-assert exec bit (can be lost during CI releng-airootfs merge).
ExecStartPre=/bin/sh -c 'chmod +x /usr/local/bin/darkos-tty1-login || true'
ExecStart=-/usr/local/bin/darkos-tty1-login %I $TERM
Type=idle
