#!/bin/bash
# DarkOS live ISO — first-boot automated setup script.
# Runs as root on the installed system after Calamares completes.
# Placeholder: real automation hooks are added as phases are built.

echo "DarkOS first-boot setup complete."
